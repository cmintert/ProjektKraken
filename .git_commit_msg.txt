test: fix multiple GUI, temporal and raster test failures; update tests to match current APIs and UI text

Summary:
- Fixed viewport update mode behavior in src/gui/widgets/map/map_graphics_view.py to support overlay behavior and aligned tests.
- Ensured SetRasterMappingCommand triggers maps reload in src/app/data_handler.py.
- Added convenience aliases to EventEditorWidget (`date_edit`, `end_date_edit`, `duration_widget`) in src/gui/widgets/event_editor.py for backwards compatibility with tests and integrations.
- Updated tests to reflect renamed API `_build_layer_model` → `rebuild_layer_model` and corrected MapDataBuffer usage in raster tests.
- Adjusted expectations for mode indicator text in map widgets (now uses "● Normal").

Verification:
- Ran targeted pytest groups and large test batches locally; previously failing tests now pass in targeted runs; broad non-unit run reported 236 passed, 3 warnings. Recommend running full CI to confirm platform parity.

Notes:
- Changes were minimal and focused on resolving test mismatches and emitting correct reload signals. Please review updated tests for alignment with intended UI text and APIs.
