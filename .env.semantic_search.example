# Semantic Search Configuration Example
# Copy this file to .env or export these variables in your shell

# =============================================================================
# Embedding Provider Configuration
# =============================================================================

# Provider: 'lmstudio' (default) or 'sentence-transformers'
EMBED_PROVIDER=lmstudio

# =============================================================================
# LM Studio Configuration (when using lmstudio provider)
# =============================================================================

# LM Studio API endpoint
# Default: http://localhost:8080/v1/embeddings
LMSTUDIO_EMBED_URL=http://localhost:8080/v1/embeddings

# Model name for embeddings
# Must match a model loaded in LM Studio that supports embeddings
# Examples: bge-small-en-v1.5, bge-base-en-v1.5, e5-small-v2
LMSTUDIO_MODEL=bge-small-en-v1.5

# Optional API key (if your LM Studio setup requires authentication)
# LMSTUDIO_API_KEY=your-api-key-here

# =============================================================================
# Advanced LM Studio Configuration (optional, for non-standard setups)
# =============================================================================

# Request/response shape customization (rarely needed)
# LMSTUDIO_INPUT_KEY=input
# LMSTUDIO_MODEL_KEY=model
# LMSTUDIO_EMBED_PATH=data[].embedding

# =============================================================================
# Sentence-Transformers Configuration (when using sentence-transformers provider)
# =============================================================================

# Model name for sentence-transformers
# The LMSTUDIO_MODEL variable is reused for sentence-transformers
# Examples: all-MiniLM-L6-v2, paraphrase-MiniLM-L6-v2
# LMSTUDIO_MODEL=all-MiniLM-L6-v2

# =============================================================================
# Usage Examples
# =============================================================================

# After setting these variables, you can use the semantic search CLI:
#
# Rebuild index:
#   python -m src.cli.index rebuild --database world.kraken
#
# Query:
#   python -m src.cli.index query --database world.kraken --text "wizard"
#
# Index a single object:
#   python -m src.cli.index index-object --database world.kraken \
#     --type entity --id <uuid>

# =============================================================================
# Model Recommendations
# =============================================================================

# For general use (fast, good quality):
#   - bge-small-en-v1.5 (384 dimensions)
#   - all-MiniLM-L6-v2 (384 dimensions)
#
# For better quality (slower, larger):
#   - bge-base-en-v1.5 (768 dimensions)
#   - e5-large-v2 (1024 dimensions)
#
# Trade-offs:
#   - Larger dimensions = better quality, more storage, slower queries
#   - Smaller dimensions = faster, less storage, lower quality
